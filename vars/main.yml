# distribution and version concatenated string used by NVIDIA and Amazon for some packages
# Ubuntu 22.04 -> ubuntu2204
nvidia_distroversion: "{{ (ansible_distribution ~ ansible_distribution_version) | replace('.', '') | lower }}"
